<section class="relative overflow-hidden">
  <div class="absolute inset-0 -z-10">
    <video
      class="h-full w-full object-cover opacity-35"
      autoplay
      muted
      loop
      playsinline
      preload="metadata"
    >
      <source src="/video/hero.mp4" type="video/mp4" />
    </video>

    <!-- dark overlay so text reads -->
    <div class="absolute inset-0 bg-gradient-to-b from-black/80 via-black/70 to-black"></div>
  </div>

  <div class="mx-auto max-w-6xl px-6 py-20 sm:py-28">
    <p class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-sm text-white/80">
      <span class="h-2 w-2 rounded-full bg-red-600"></span>
      Managed IT for growth-minded local businesses
    </p>

    <h1 class="mt-6 max-w-3xl text-4xl font-bold tracking-tight sm:text-6xl">
      Modern IT support that keeps your business moving.
    </h1>

    <p class="mt-5 max-w-2xl text-lg leading-relaxed text-white/70">
      RedGate-IT helps small teams modernize their networks, improve security, and move confidently to Microsoft 365.
    </p>

    <div class="mt-8 flex flex-col gap-3 sm:flex-row">
      <a href="#contact" class="inline-flex items-center justify-center rounded-xl bg-red-600 px-5 py-3 font-semibold hover:bg-red-500">
        Book a consultation
      </a>
      <a href="#services" class="inline-flex items-center justify-center rounded-xl border border-white/15 bg-white/5 px-5 py-3 font-semibold text-white/90 hover:bg-white/10">
        View services
      </a>
    </div>
  </div>
</section>
